const isChecked = true,
      isClose = true;

console.log(isChecked && isClose); // && - и  // работает только когда все аргументы правдивые
console.log(isChecked || isClose); // || - или // работает если хотя бы один аргумент правдивый

// ! - оператор отрецания "делает все наоборот" если true => false , если false => true

